$('#comment').html("<%= j(render "book_comments/bookComment", book: @book ) %>");
$('#form').html("<%= j(render "book_comments/form", book: @book, comment: @comment ) %>");
$('#comment_count').html("コメント数：<%= @book.book_comments.count %>");
$('textarea').val('');